// Public surface of the shared package. Real modules (types, assessment configs,
// detection, mapping, validation) are exported here as they land in Phase 2.

export const SHARED_PACKAGE = "@assessment/shared";
